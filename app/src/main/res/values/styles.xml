<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- TextAppearance -->
    <style name="TextAppearanceSize.Large" parent="@android:style/TextAppearance">
        <item name="android:textSize">42sp</item>
        <item name="android:textStyle">bold|italic</item>

    </style>

    <style name="TextAppearanceSize.Medium" parent="@android:style/TextAppearance">
        <item name="android:textSize">36sp</item>
    </style>

    <style name="TextAppearanceSize.Small" parent="@android:style/TextAppearance">
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="TextAppearance.Date" parent="@android:style/TextAppearance">
        <item name="android:textSize">10sp</item>
        <item name="android:textStyle">italic</item>
        <item name="android:textColor">@color/grayTextColor</item>
    </style>

    <style name="EditTextHintAppearance" parent="TextAppearance.AppCompat">
        <item name="android:textColor">@color/blackTextColor</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textColorHint">@color/blackTextColor</item>
    </style>

    <style name="ThemeOverlay.AppCompat.EditTextTheme">
        <item name="colorControlNormal">@color/blackTextColor</item>
        <item name="colorControlActivated">@color/blackTextColor</item>
        <item name="colorControlHighlight">@color/colorAccent</item>
    </style>


    <style name="ThemeOverlay.AppCompat.navTheme">
        <!-- Color of text and icon when SELECTED -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!-- Background color when SELECTED -->
        <item name="colorControlHighlight">@color/navSelectedBackground</item>

    </style>

</resources>
